extends Node

signal ls_health_changed(new_health, max_health)
signal ls_energy_changed(new_energy, max_energy)
signal ls_level_changed(level)
signal ls_enemy_died(enemy_id,cur_level)


func _ready() -> void:
	pass 

func _process(delta: float) -> void:
	pass
